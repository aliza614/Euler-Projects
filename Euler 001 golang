package main

import (
	"fmt"
)

func main() {

    answer:=0
    for i:=1; i<1000;i++{
       if i%3==0 {
          answer+=i
       } else if i%5==0 {
          answer=answer+i;
       }
    }
    fmt.Println(answer);
}
